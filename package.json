{
  "name": "heisenberg",
  "version": "0.1.0",
  "private": true,
  "description": "Heisenberg v1 — quantum browser IDE platform (Node-side workspace root).",
  "license": "Apache-2.0",
  "author": "Nimesh Cheedella <chnimesh0808@gmail.com>",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint && pnpm --filter web typecheck",
    "test": "pnpm --filter web test"
  }
}
